<!DOCTYPE html>
<html>
  <head>
    <title>ieieRubyStudy</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  </head>
  
  <body class="container">
    <header>
      <nav class="navbar navbar-expand-sm navbar-dark bg-info">
        <ul class="navbar-nav">
          <li class="nav-item active">
            <a href="/" class="navbar-brand">Top</a>
          </li>
          <li class="nav-item active">
            <a href="/students/index" class="nav-link">一覧・検索</a>
          </li>
          <li class="nav-item active">
            <a href="/students/new" class="nav-link">追加</a>
          </li>
          <li class="nav-item active">
            <a href="/users/logout" class="nav-link">ログアウト</a>
          </li>
        </ul>
      </nav>
    </header>
    <% if flash[:notice] %>
      <div class="text-white-40 bg-warning">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <% if session[:user_id] %>
      <h5 class="text-right"><%= "#{@user.account}がログイン中" %></h5>
    <% end %>

    <main class="mt-3">
      <%= yield %>
    </main>
    
    <footer>
      <hr class="mt-5">
      <p class="text-center">Copy-right ieie</p>
    </footer>
</body>
</html>
